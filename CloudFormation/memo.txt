VPC ⇒ Public or Private EC2 Instance ⇒ RDS Instance
CW Agentsでログとるところまで設定完了。

⇒コマンドをUserDataに記載してCWをインストール&起動する方法。
　
　SSM中のRUN画面で、ドキュメント参照してEC2インスタンスを指定してコマンドでインストールすることもできる。
　(インストール&起動は別ドキュメントにそれぞれコマンドまとまってる)
　
⇒コマンドが成功すれば、CWメトリクス内にユーザー指定のメトリクスが作成される。
　RUN画面でCWAgentsの状態を確認することも可能。
　

有線だと接続できなくなった
(特定のサイトのみ。Youtube, Gmail、Quiitaなどは接続可能。AWSコンソールのログイン画面、Discord, Steamログイン不可。ルーター再起動、PC本体の再起動、キャッシュクリア試すも改善せず)

無線LAN子機を使用したところ、正常に有線で接続できなかったサイトに接続可能に。

Azure, AWS, GCPでメールアドレスバレる。＝本名バレる。
メールアドレスを変更する。他のメアドを作成して、これまでのメアドから他のメアドに転送されるようにする?

CloudTrailのログをCWLogGroupにリンク。(特定のサービス、リソース、アクションのログのみ送ることが可能)
⇒このLogGroupの情報を参照して、特定のログの場合を検出した場合にLambda functionを起動してSNS経由でメールを出すように調整してみる。SNSからLambda関数で別のこともできて、SNSトピックからメール出せる。
　⇒マネコンで設定し完了。これをCfnでやってみる。


Cfnで別リージョンのVPCどうしのpeeringやってみる。
⇒データ(Packet)のやりとりできるか? 別リージョンどうしのインスタンスで疎通確認できればOKとみなす。

　⇒EFSにしてインスタンスに結合させて、DataSyncでSource, Destination ARNを入れればいけそう。